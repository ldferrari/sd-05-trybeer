import React from 'react';

const Card = (prop) => (
  <div className="product-card">
    {/* foto
    nome
    preco uni
    qtd atual
    add button
    rem button */}
    {prop.name}
  </div>
);
export default Card;
